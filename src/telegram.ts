import type { Env, ItemRow } from './types';
function configured(env:Env):env is Env&{TELEGRAM_BOT_TOKEN:string;TELEGRAM_CHAT_ID:string}{return Boolean(env.TELEGRAM_BOT_TOKEN&&env.TELEGRAM_CHAT_ID);}
async function send(env:Env,text:string,topicId?:string):Promise<boolean>{if(!configured(env))return false;const body:Record<string,unknown>={chat_id:env.TELEGRAM_CHAT_ID,text,parse_mode:'HTML',disable_web_page_preview:true};if(topicId)body.message_thread_id=Number(topicId);const r=await fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/sendMessage`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});return r.ok;}
function h(value:string):string{return value.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');}
export async function pushP1(env:Env,item:ItemRow&{source_name?:string}):Promise<boolean>{const labels:Record<string,string>={free_credit:'免费额度',limited_offer:'限时优惠',price_drop:'降价',price_change:'价格变化',new_plan:'新套餐',other:'重要信息'};const lines=[`🔥 <b>${h(labels[item.kind]||'重要优惠')}</b>`,'',`<b>${h(item.title)}</b>`];if(item.summary)lines.push(h(item.summary.slice(0,700)));lines.push('',`来源：${h(item.source_name||'unknown')}`);if(item.verification_status==='official_confirmed')lines.push('核验：官方确认');if(item.expires_at)lines.push(`有效期：${h(item.expires_at)}`);if(item.url)lines.push('',`<a href="${h(item.url)}">查看原文</a>`);return send(env,lines.join('\n'),env.TELEGRAM_TOPIC_P1);}
export async function pushDailyReport(env:Env,reportDate:string,counts:{p2:number;p3:number}):Promise<boolean>{const base=env.PUBLIC_BASE_URL.replace(/\/$/,'');const url=base?`${base}/daily/${reportDate}`:'';const text=[`📰 <b>AI-Radar 日报 · ${h(reportDate)}</b>`,'',`值得关注：${counts.p2} 条`,`其他线索：${counts.p3} 条`,url?'':undefined,url?`<a href="${h(url)}">打开今日完整日报</a>`:undefined].filter(Boolean).join('\n');return send(env,text,env.TELEGRAM_TOPIC_DAILY);}
